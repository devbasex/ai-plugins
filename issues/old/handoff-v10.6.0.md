# 引継ぎ: v10.6.0 の配布と振り返り

**この文書は v10.6.0 の引継ぎであり、2026-09-07 に完了した。** 残りの工程は下の
「完了条件」にあるとおりすべて済んでいる。

## いまどこにいるか

**すべて完了した。**

| 工程 | 状態 |
| --- | --- |
| 実装（5 件） | **完了。**マイルストーン v10.5.1 と v10.5.2 はどちらも closed（open 0 件） |
| 検証への配布 | **完了。** `develop` が `10.6.0-dev.1` を配っている |
| リリース後テスト | **完了。** Claude Code と Codex で実機確認済み |
| 振り返り | **完了。** PR #454 のコメント 1 件に記録 |
| 本番への配布 | **完了。** `main` が `10.6.0` を配っている（Release PR #454） |

`develop` の先端は `1c426f3`、`main` は `7883bbb`。両者の差分は空である。作業ツリーは 1 つも残っていない。

## 完了したもの

| 課題 | 内容 | Pull Request |
| --- | --- | --- |
| #421 | `architecture` を廃止し `standard` へ改名 | #435 |
| #438 | `refactor.py`（3706 行）を `refactor_lib/` の 13 モジュールへ分割 | #439 |
| #436 | `cross-refactoring` を 4 層のラウンドへ。レビューをテストへ置き換え | #437（設計）/ #447 |
| #423 | 運用モード `operation` の新設 | #448 |
| #391 | `document-restructuring` の新設、工程名 2 件の改名 | #449 |
| 版上げ | `10.5.1` → `10.6.0-dev.1` | #450 |

**設計は 3 件を 1 本にまとめた**（#434 でマージ済み。`issues/issue-421-423-391-modes-and-stages/`）。

## 残っている作業

### 1. 控えの工程名を移行する

**課題の本文（18 件）は移行したが、ローカルの控えは旧い名前のまま残っている。**

```console
$ bash plugins/ndf/skills/development-workflow/scripts/stage-check.sh report 391
#391 の通過工程（standard）
  記録あり: 要求と受け入れ条件 / 作業場所の用意 / 設計 / 計画 / 実装 / 後片付け / 配布 / リリース後テスト
  記録なし: ドキュメント再構成 / ドキュメントレビュー / 構造改善 / 実装レビュー / 完了判定 / Pull Request / 確定仕様化
```

**「記録なし」に出ている工程は、実際には通っている。** 控えが `設計レビュー` / `レビュー` の
まま記録されており、新しい名前では見つからない。

| 対象 | 場所 |
| --- | --- |
| 控え | `~/.local/state/ndf/stages/devbasex__ai-plugins__<課題番号>.json`（16 件） |
| 書き換え | `stages` 配列の `設計レビュー` → `ドキュメントレビュー`、`レビュー` → `実装レビュー`、`mode` の `architecture` → `standard` |

**設計では「控えは実行のたびに作り直されるため放置してよい」としたが、これは誤りだった。**
控えは課題ごとに残り、報告に影響する。**この食い違いを振り返りに含める。**

### 2. 振り返り（`/ndf:retrospective`）

**記録すべき知見が集まっている。** とくに次の 2 つは、この作業でしか得られない実測である。

#### 追随漏れが繰り返し出た

**工程やモードを 1 つ足すたびに、追随先の漏れがレビューで見つかった。**

| 変更 | 漏れた件数 | 漏れた先 |
| --- | ---: | --- |
| #423（モードを 1 列足す） | 3 | 標準フロー図 / `pr` と `merged` の関門の名前 / `retrospective` の条件 |
| #391（工程を 1 行足し、2 件改名） | 6 | 記録の呼び出し / 確定仕様 2 か所 / 控えと報告の例 / テストの工程名リスト / 数を書いた記述 5 か所 |

**事前に洗い出した 21 ファイルでは足りなかった。** 実際の追随先は 28 ファイル + 記録の
呼び出し + 確定仕様 2 本 + 例示 2 か所 + 数を書いた記述 5 か所だった。

**再発しない直し方は 2 つだけだった。**

| 直し方 | 内容 | 再発するか |
| --- | --- | --- |
| 値を足す | 記録の呼び出し・確定仕様・例示 | **する** |
| **工程表から読む** | テストが工程名を写さず、`SKILL.md` の表を読む | しない |
| **数を書かない** | 「15 行」「4 つのモード」をやめる | しない |

#### 分割を先に行った判断

**利用者の指摘で、#436 の実装より前に #438（分割）を行った。**

| 事実 | 内容 |
| --- | --- |
| きっかけ | 実装計画の 10 タスクのうち **9 つが 3706 行の 1 ファイル**を触ると分かった |
| **私の失敗** | リスクとして書いたが、**対処の選択肢に「先に構造を整える」が入っていなかった** |
| 実測（1） | 依存を測ると**循環が 1 つ**見つかった。責務のコメントは依存を反映していなかった |
| 実測（2） | 分割のレビューは **1 ラウンドで収束**（テストの差分 0 件が機械で確かめられる） |
| **手戻り** | **分割前に Task 1 を実装してコミットしており、破棄した**（1 回のやり直し） |

**この経緯は #442 として起票済み。** 振り返りでは、**手戻りの実測**を残す。

#### E4（新しい cross-refactoring の実機確認）

`issues/issue-436-cross-refactoring-flow/03-field-test.md` に記録済み。

| 指標 | 旧（PR #435） | 新（PR #448） |
| --- | --- | --- |
| レビューの起動 | 8 回 / 1843 秒 | **0 回 / 0 秒** |
| 取り消し | 18 項目のうち 9 件 | **0 件** |

**取り消しの主因だった「範囲外への変更」は、`--scope` の関門が実行前に止めた。**

### 3. 本番への配布（完了）

**2026-09-07 に承認を得て実施した。** 手順は `/ndf:release` の「本番のチャネルへ届く操作」の
手順に従う。

| 手順 | 内容 |
| --- | --- |
| 1 | 接尾辞を外して `10.6.0` にする（版数を持つ 15 箇所） |
| 2 | `develop` から `main` への Pull Request を作る |
| 3 | **承認を得てから** `gh pr merge <番号> --merge --admin` |
| 4 | `claude plugin tag plugins/ndf --push` でリリースタグを打つ |
| 5 | `git diff --stat origin/develop origin/main` が空であることを確かめる |

**`git push origin develop:main` は使えない**（ruleset の bypass は `pull_request` モード）。

## 破壊的変更（配布の提示物）

**3 つある。いずれも後方互換の仕組みを持たない。**

| 変更 | 内容 |
| --- | --- |
| モード名 | `architecture` を廃止し `standard` へ統合。`operation` を新設 |
| 工程名 | 設計レビュー → **ドキュメントレビュー**、レビュー → **実装レビュー** |
| 副コマンド | `cross-refactoring` の `judge-review` / `review-targets` を削除 |

**取り消しの手段**: こちらから前の版へ戻す手段は無い。利用者の側で取得元をタグへ固定するか、
別名のマーケットプレイスで対象だけ固定する。**サードパーティのマーケットプレイスは自動更新が
既定で無効**なので、`marketplace update` を実行した利用者から順に届く。

## 他のリポジトリへの移行

**2 件へ課題を投稿済み。** 移行の内容と実測した件数を追記してある。

- https://github.com/takemi-ohama/with-bi/issues/94（`## 進行` の節 4 件）
- https://github.com/volareinc/car-pricing/issues/200（同 9 件）

## v10.5.3 へ回した課題（6 件）

**いずれもこの作業から見つかった実在の問題である。** 主題は「リファクタリングの方法論と構造」で
まとまっている。

| 課題 | 内容 |
| --- | --- |
| #440 | 分割の段階 2（テストをモジュール境界へ寄せる） |
| #441 | 分割の段階 3（再エクスポートと `__setattr__` を外す） |
| #442 | 実装の前に構造改善を行うかを判断する工程 |
| #443 | 「テストを変えない」の位置づけを定める |
| #444 | `refactoring` と `cross-refactoring` の役割を分け直す |
| #446 | `cross-refactoring` からレビュー機構の残骸を取り除く |

**#445**（`check-markdown-links.py` が同一ページのアンカーを検査していない）はマイルストーン
未設定。

## 完了条件

**すべて満たした（2026-09-07）。**

- [x] 1. 控えの工程名が新しい名前へ移行されている
  - 16 件のうち 10 件を書き換えた（`設計レビュー` → `ドキュメントレビュー` / `レビュー` →
    `実装レビュー` / `mode` の `architecture` → `standard`）
  - **残る「記録なし」は名前の不一致ではなく、記録そのものが無い。**#391 と #423 の
    実装レビュー・完了判定・Pull Request がこれにあたる。控えを埋めて辻褄を合わせることは
    しない（記録が無いことと、工程を通っていないことは別である）
- [x] 2. `/ndf:retrospective` が済み、記録が残っている
  - 記録の本体: https://github.com/devbasex/ai-plugins/pull/454#issuecomment-5563701891
  - 起点の 5 件（#391 / #421 / #423 / #436 / #438）の本文末尾へ、辿る経路の 1 行を足した
  - **まとまりであるため、記録の本体は配布した Pull Request へ置いた**（`retrospective` の
    投稿先の表）
- [x] 3. 振り返りで起票し損ねた課題を拾った
  - **#451** 控えの工程名の移行が更新の案内に無く、通った工程が「記録なし」として出る
    （`priority: high` / v10.5.3）
  - **#452** 進行の記録が課題の本文と控えで食い違っても、どちらも気づかない
    （`priority: medium` / マイルストーン未設定）
- [x] 4. 本番への配布を行った（利用者の承認を得た）
  - `main` = `7883bbb`（Release PR #454）。`git diff --stat origin/develop origin/main` は空
  - リリースタグ `ndf--v10.6.0` を `main` の先端へ打った
  - 版数 15 箇所の接尾辞を外した（PR #453）

## 手入れした課題（`issue-upkeep`）

| 課題 | 判定 | 反映 |
| --- | --- | --- |
| #375 | 書き直しが要る | モードの母集合を現行（`light` / `operation` / `legacy-refactor` / `standard`）へ組み直した |
| #158 | 追記が要る | `AUTH_PROBES` の位置を `plugins/ndf/scripts/lib/auth.py` へ直した |
| #451 | マイルストーン未設定 | v10.5.3 へ入れ、説明へ番号を足した |
| #341 / #333 / #347 / #446 | そのまま | `architecture` は英語の一般語、`refactor.py` は現存する |

**未設定のまま残した課題は 3 件である。** #452（中くらい。主題の合うまとまりが無く、
1 件では新しく作らない）、#445 と #415（低い）。
