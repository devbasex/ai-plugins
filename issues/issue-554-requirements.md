# #554: エージェント向け指示書を適切に保つ検査を NDF から配る

設計は [issue-554-design.md](issue-554-design.md) にある。この文書は「何を満たすか」だけを扱う。語の意味は末尾の「用語」にある。

## この課題でしたいこと

**NDF を入れたリポジトリで、エージェント向けの指示書を適切に保てるようにしたい。** 指示書は
`AGENTS.md`（Codex / agy）・`CLAUDE.md`・`KIRO.md` の 3 種で、**そこに書いたものは全セッションと
全サブエージェントが毎回読む**。何を書いてもよい場所ではなく、**毎回の読み込みの量をそのまま増やす場所**である。
利用者は NDF を入れた任意のリポジトリで、`devbasex/ai-plugins` はその 1 例にすぎない。

**いま指示書を機械で見ているものは無い。** 指示書は増える一方で、次の 3 つが起きても誰も止めない。

| 起きること | いまの結果 |
| --- | --- |
| 出た版の判断が指示書へ残り続ける | 毎回の読み込みに乗り続ける。`ai-plugins` では 333 行のうち 79%（264 行）がこの状態になった（#551） |
| 即時読み込みの参照（`@<パス>`）が増える | 参照先の中身も毎回読み込まれる。**参照先が消えていても気づかない** |
| 指示書の総量が増える | 気づく手段が無い。行数の検査を持つリポジトリでも、1 本ずつの行数しか見ていない |

**この課題で配るのは、その 3 つを見る検査である。** 配布物（`plugins/ndf/`）に置き、4 ランタイムのどれで
入れても同じ実体が動く。判定の強さは**リポジトリ側の宣言**で決まる。

| 宣言 | 動く判定 |
| --- | --- |
| 宣言が無い | 壊れた即時読み込み（参照先が無い）で落ちる。指示書の本数と読み込みの量を報告する |
| 宣言がある | それに加えて、許可していない即時読み込み・出た版の段落・読み込みの量の上限で落ちる |

目的を 3 つにまとめると次になる。

- 指示書の壊れた即時読み込みは、宣言の有無によらず落ちる
- 版の判断を指示書へ書くリポジトリでは、出た版の段落が残ったまま Pull Request を出すと落ちる
- 毎回の読み込みの量が、実行するたびに数字で出る

## 実例: この検査が捕まえるもの（`ai-plugins` の過去の状態）

**動機と、検査が何を捕まえるかは `ai-plugins` の過去の状態で示せる。** これは適用の 1 例であり、判定の前提ではない。

| 見るもの | `f56c90d9`（PR #552 の直前）の値 |
| --- | --- |
| `CLAUDE.md` の行数 | 333（行数の検査は 501 行以上で落とすため**通っていた**） |
| 行頭が出た版の版数で始まる段落 | **7 個**（`v10.0.0` 〜 `v10.5.1`。当時の最新は `10.9.1`） |
| 即時読み込みの参照 | `@AGENTS.md` と **`@docs/ndf-version-decisions.md`**（出た版の記録そのものを毎回読ませていた） |

この 8 件を退避した結果、毎回の読み込みが **19,186 トークン（30.3%）減った**（PR #552 の実測）。
**同じ状態へ戻ったときに機械が止めるのが、この課題の狙いである。**

## 前提

- 前提 1: 実行するのはリポジトリの作業ツリーの中で、`git` が使える
- 前提 2: 即時読み込みの一致範囲は Claude Code 2.1.274 で実測した範囲とする（設計文書の「実測」）。他のランタイムが同じ記法を読むかは測っていない
- 前提 3: 指示書の既定の名前は `AGENTS.md` / `CLAUDE.md` / `KIRO.md` の 3 種とする。増減はリポジトリ側の宣言で行う
- 前提 4: 宣言はリポジトリの `.ndf/` に置く（`worktree.json` / `projects.json` と同じ場所と同じ考え方）

## 対象範囲

含む:

- 配布物（`plugins/ndf/scripts/`）に置く検査 1 本と、その単体テスト
- リポジトリ側の宣言（`.ndf/instructions.json`）の読み取りと、宣言が無いときの既定の振る舞い
- 走査する指示書の決め方（根と配下、追跡されているものだけ）
- 判定 4 つ（壊れた即時読み込み / 許可していない即時読み込み / 出た版の段落 / 読み込みの量）
- 既存 Skill（`release`）の退避の手順から検査を呼ぶこと
- 宣言の書き方と判定の一覧を書いた参照 1 本
- **このリポジトリへの適用**（宣言・継続的統合のジョブ・必須の検査・手順の記載）。配布物とは別の節に置く

含まない:

| 扱わないもの | 理由 |
| --- | --- |
| 新しい Skill を足すこと | 設計文書の決定 1。実行するのは 1 コマンドで判断が要らない。Skill を足すと説明文が全セッションの固定費に乗る |
| 指示書どうしの重複した段落の検出 | 決定 8。同じ規約を意図して再掲する書き方があり、機械では区別できない |
| ランタイム間で内容が食い違うことの検出 | 決定 8。同じ意味かどうかの判定になる |
| Markdown のリンク切れ | 決定 8。即時読み込みではないため毎回の読み込みに乗らない。リンクの検査を持つリポジトリの領分 |
| 読み込みの量をトークン数で測ること | 決定 7。CLI の認証と課金が要り、読み込む設定で値が変わる。バイト数で測る |
| 指示書の中身を直すこと（自動の退避） | 移す先と位置は人が決める。検査は場所と直し方を示すまで |
| `.kiro/steering/` や `.claude/` の下の設定 | 指示書の名前を持たない。宣言の `files` へ書けば対象になる |
| このリポジトリの `scripts/` 側への検査の新設 | 配布物の側に置くため。このリポジトリは配布物を呼ぶ |

## 検査が落ちたときに何をするか

| 指摘 | 直し方 |
| --- | --- |
| 参照先の無い即時読み込み | 参照を消すか、実在するパスへ直す |
| 許可していない即時読み込み | `@` を外す（リンクかコードスパンにする）。毎回読ませる必要があるなら、宣言の `imports` へ理由とともに足す |
| 許可の指し先が無い | 宣言の `imports` からその行を外す |
| 出た版の段落が指示書に残っている | その段落から次の見出しまでを、宣言の `decisions` が指す退避先へ移す |
| 版が決まる前の段落が、配布を越えて残っている | 書き出しを出す版の版数へ直し、同じく退避先へ移す |
| 読み込みの量が上限を超えた | 現行でない記述を退避先へ移す。上限そのものを変えるなら宣言を直す |

**版が決まる前の段落は、宣言した印（既定は無し）で書き出す。** 版数はまとまりをマージするまで決まらないため、
書く時点では直前の正式版しか書けない。印を宣言したリポジトリでは、配布で新しい版が出た時点で検査が拾う。

## 受け入れ条件

### 走査の対象

- [ ] AC1: 宣言の無いリポジトリで実行すると、根の `AGENTS.md` / `CLAUDE.md` / `KIRO.md` と配下の同名のファイルを走査し、走査した本数を出力へ出す
- [ ] AC2: `git` が追跡していないファイルは走査に入らない
- [ ] AC3: 指示書が 1 本も無いリポジトリでは、対象が無いことを 1 行出して終了コード 0 で終わる。ただし版を取れない宣言があるときは終了コード 2 が優先する
- [ ] AC4: 宣言の `files` に名前を足すと、その名前のファイルも走査に入る

### 宣言が無くても動く判定

- [ ] AC5: 参照先の存在しない `@docs/none.md` が指示書にあると、終了コード 1 で `ERROR: <ファイル>:<行>:` の形の指摘が出る
- [ ] AC6: 参照先は、その参照を書いた指示書のディレクトリから解決する（`docs/AGENTS.md` の `@x.md` は `docs/x.md` を指す）
- [ ] AC7: `@~/x.md` と、作業ツリーの外へ出る参照は存在を判定しない（許可の判定には掛ける）
- [ ] AC8: 追跡された symlink が根の外を指すとき、その参照先は読まず、存在も判定せず、量にも入れない
- [ ] AC9: コードスパン（`` `@openai/codex` ``）とコードブロックの中の `@` は指摘されない
- [ ] AC10: メールアドレスの形（`user@example.com`）と、括弧の直後の `@`（`(@x.md)`）は指摘されない
- [ ] AC11: 強調の中（`**@AGENTS.md**`）と空白の直後の `@AGENTS.md` は参照として読む
- [ ] AC12: 根の指示書ごとに、その本文と `@` でたどった先の合計バイト数が 1 行ずつ出る。多段の参照をたどり、同じ参照先は 1 回だけ数え、循環する参照でも止まる。`--report` を付けると内訳が出る
- [ ] AC13: 宣言が無いとき、出た版の判定と許可の判定は動かず、動かないことが出力に出る

### 宣言があるときの判定

- [ ] AC14: 宣言に `$schema` を書いても、書かない宣言と同じに読まれる（読み取り側は参照しない）
- [ ] AC15: `imports` を宣言すると、許可の外の `@` は参照先が存在しても終了コード 1 になる。キーはリポジトリの根からの相対パスで、同じ名前の別の指示書には効かない
- [ ] AC16: `"imports": {}`（許可が 1 つも無い）を書くと、参照がすべて指摘される。未指定のときは許可の判定が動かない
- [ ] AC17: `imports` の指し先が存在しないと終了コード 1 になる（宣言の陳腐化）。存在を判定しないパス（`~`・作業ツリーの外）の許可は、存在しないことを理由に指摘しない
- [ ] AC18: `released` に変更履歴の文書と見出しの形を宣言すると、最新以下の版数で書き出した段落と見出しが指摘される
- [ ] AC19: 段落の 2 行目以降が版数で始まっても指摘されない（折り返した行は段落の書き出しではない）。連続する箇条書きの 2 件目以降は段落の先頭として指摘される
- [ ] AC20: 同じ宣言で、最新より新しい版数で始まる段落は指摘されない
- [ ] AC21: `released` にタグの形を宣言すると、タグの最大値を最新として同じ判定をする
- [ ] AC22: `released` の宣言から版を 1 つも取れないとき、走査の前に終了コード 2 で終わる（壊れた即時読み込みの指摘も出さずに止まる）
- [ ] AC23: `pending_marker` を宣言すると、その印で書き出した段落は基底が最新**未満**のときだけ指摘される
- [ ] AC24: 出た版の段落の指摘に、宣言の `decisions` が指す退避先が出る
- [ ] AC25: `budget.bytes` を宣言し、**根の指示書 1 本の**合計がそれを超えると終了コード 1 になる
- [ ] AC26: 宣言の構造が不正なとき、終了コード 2 で終わる。対象は JSON として読めない・`version` が無いか未対応・項目の型が違う（`files` が配列でない）・`pattern` が正規表現として読めないの 4 つ

### 出力と終了コード

- [ ] AC27: 指摘は 1 件 1 行で、行を持つものは `ERROR: <ファイル>:<行>: <理由>`、持たないものは `ERROR: <理由>` になる
- [ ] AC28: 指摘が 1 件も無ければ終了コード 0 で、走査した本数と根の指示書ごとの読み込みの量が標準出力に出る
- [ ] AC29: 知らない引数を渡すと終了コード 3 で終わり、使い方が標準エラーへ出る

### 配布と規約

- [ ] AC30: 検査の実体は `plugins/ndf/scripts/` にある。新しい Skill は足さない（配る先の一覧の行数が変わらない）
- [ ] AC31: `release` の退避の手順から検査を呼ぶ記載があり、宣言の書き方と判定の一覧を書いた参照が 1 本ある。呼び出しは `$SCRIPTS` を解決してから行う形で書かれている
- [ ] AC32: 4 ランタイムの導入先それぞれからスクリプトを起動し、一時リポジトリを `--root` に渡して終了コード 0 になる
- [ ] AC33: `python3 scripts/check-skill-repo-assumptions.py` が終了コード 0 のまま（Skill の本文にこのリポジトリ固有の語を書かない）
- [ ] AC34: 生成物の確認と配布物の検証が終了コード 0。4 ランタイムへ同じ実体が届く（`bash scripts/build-runtime-plugins.sh --check` / `bash scripts/validate-runtime-plugins.sh`）

### このリポジトリへの適用

- [ ] AC35: `.ndf/instructions.json` を置き、リポジトリの根で検査を走らせると終了コード 0 になる
- [ ] AC36: `f56c90d9` の指示書と `CHANGELOG.md` を一時ディレクトリへ展開し、同じ宣言で走らせる。終了コード 1 で、出た版の段落 7 件と許可していない即時読み込み 1 件の計 8 件が出る
- [ ] AC37: `.github/workflows/runtime-plugin-validate.yml` に検査のジョブがあり、Pull Request では絞り込まずに起動する
- [ ] AC38: そのジョブが実装の Pull Request で pass する
- [ ] AC39: `CLAUDE.md` の「版ごとの判断の記録」に、版が決まる前の書き出しの印と、検査のコマンドが書かれている
- [ ] AC40: `docs/versioning-and-distribution.md` の「バージョン更新時の手順」に検査のコマンドがある。手順 4 は判断の理由の置き場所を `docs/ndf-version-decisions.md` と書く
- [ ] AC41: 同じ文書の「必須の検査 11 個」の記載が、ruleset へ `instruction-files-check` を足すのと**同じ時点で**新しい数になる（実装 Pull Request の差分には入れない）
- [ ] AC42: `CHANGELOG.md` の冒頭が、判断の理由の置き場所を `docs/ndf-version-decisions.md` と書く

### 退行しないこと

- [ ] AC43: 既存の検査 5 本が終了コード 0 のまま。対象は `check-doc-line-limit.py` / `check-markdown-links.py` / `check-doc-staleness.py` / `check-skill-repo-assumptions.py` / `check-skill-frontmatter.py`
- [ ] AC44: `uv run --with pytest pytest scripts/tests plugins/ndf -q` で、足したテストを含めて失敗が増えない

## 非機能の条件

| 大項目 | 条件 |
| --- | --- |
| 運用・保守性 | 指摘は 1 件 1 行で、直し方（退避先・宣言の項目）を含む。外部への通信・CLI の認証を要さない |
| 移行性 | 宣言が無いリポジトリでも動き、既定の判定だけが働く。既存の利用者が宣言を書くまで落ちない |
| システム環境 | Python 3 の標準ライブラリだけで動く。`actions/checkout` の既定（浅い clone・タグなし）で、タグを使う宣言以外は動く |

## 影響

| 対象 | 影響 |
| --- | --- |
| 公開インタフェース | 配布物へスクリプト 1 本と参照 1 本が増える。Skill の数は変わらない |
| 既存の振る舞い | `release` の退避の手順に検査の実行が 1 つ増える。宣言の無いリポジトリでの振る舞いは変わらない |
| このリポジトリ | 継続的統合に検査が 1 つ増え、必須の検査になる。落ちるとマージできない |

## 検証手段

| 項目 | 手段 |
| --- | --- |
| 検査 | `python3 plugins/ndf/scripts/instructions-check.py --root .` |
| テスト | `uv run --with pytest pytest plugins/ndf/scripts/tests/test_instructions_check.py -q` |
| 過去の状態での再現（AC36） | `git archive f56c90d9 CLAUDE.md AGENTS.md KIRO.md CHANGELOG.md` を一時ディレクトリへ展開し、`git init` と `git add -A` で追跡させ、宣言を置いて `--root` で渡す（走査は追跡対象から集めるため、展開しただけでは対象が 0 本になる） |
| 配布 | `bash scripts/build-runtime-plugins.sh --check` / `bash scripts/validate-runtime-plugins.sh` |
| 継続的統合 | 実装の Pull Request の checks でジョブの結果を見る |

## 境界

| 区分 | 内容 |
| --- | --- |
| 常に行う | 既存の配布スクリプト（`plugins/ndf/scripts/`）の書き方（終了コード・`--root`・docstring に理由）に合わせる。単体テストを `plugins/ndf/scripts/tests/` へ置く |
| 確認してから行う | 必須の検査の一覧（ruleset）の変更。利用者が入れると決めたため、実行は実装 Pull Request のマージ後に進行側が行う |
| 行わない | 新しい Skill の追加、指示書の中身の自動の書き換え、このリポジトリの `scripts/` への検査の新設 |

## 未決

未決の項目は無い。必須の検査へ入れるかは利用者が決めた（入れる）。

## 用語

| 用語 | この文書での意味 |
| --- | --- |
| 指示書 | エージェントが読む規約の文書。既定の名前は `AGENTS.md` / `CLAUDE.md` / `KIRO.md` |
| 根の指示書 | リポジトリの根に置かれた指示書。セッションのたびに読まれる |
| 配下の指示書 | サブディレクトリに置かれた指示書。その場所で作業したときに読まれる |
| 即時読み込みの参照 | コードの外に書いた `@<パス>`。参照先の内容がそのまま読み込まれる |
| 出た版 | リポジトリが配布済みとして宣言した版。取り出し方（変更履歴の文書・タグ）は宣言が決める |
| 宣言 | リポジトリ側に置く `.ndf/instructions.json`。無ければ既定の判定だけが動く |
| 読み込みの量 | 根の指示書と、そこから即時読み込みでたどった先の合計バイト数 |

## 依頼（原文）

> **「開発の指示書には現行版で決めたことだけを残す」という規則を、機械で見ていない。**
>
> （中略）
>
> | 何を見るか | 判定の仕方 |
> | --- | --- |
> | 出た版の見出しが指示書に残っていないか | 配布済みの版数の一覧（変更履歴の文書またはタグ）と突き合わせる |
> | 即時読み込みの記法が使われていないか | 指示書の中の `@` 参照を数え、許可した先だけに限る |
> | 指示書の分量そのもの | 行数ではなく**読み込みの量**で見る（分量の基準は文書の種類で変わる） |

（#554 本文の「何を見つけたか」「考えられる見方」から引用。全文は課題にある）

> **NDF の Skill として、インストールされたスコープ全体のエージェント向け指示書（AGENTS.md など）を適切に保つようにしたい**

（差し戻しの依頼。配布物の側の機能として作り直すこと）
