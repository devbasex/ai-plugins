# #554: 開発の指示書に「現行版だけを残す」規則を機械で見る

設計は [issue-554-design.md](issue-554-design.md) にある。この文書は「何を満たすか」だけを扱う。語の意味は末尾の「用語」にある。

## この課題でしたいこと

**開発の指示書に残すのは、現行版で決めたことだけにしたい。** 指示書は `CLAUDE.md` / `AGENTS.md` / `KIRO.md` の 3 本で、
**全セッションと全サブエージェントが毎回読む**。出た版の判断をそこへ残すと、読み込みの量が毎回そのまま乗る。
置き場所は #551 で決まっており、出た版の判断は `docs/ndf-version-decisions.md` へ移す。#551 の退避では、
毎回の読み込みが 19,186 トークン（30.3%）減った。

**いまこの規則を守らせているのは散文 2 箇所だけである。** 規約（`CLAUDE.md` の「版ごとの判断の記録」）と配布の手順
（`release`）の 2 つで、どちらも読んだ人と AI が守る前提になっている。機械が見ているのは分量だけで、501 行を超えなければ通る。
**#551 の直前は 333 行のうち 79%（264 行）が出た版の判断だったが、既存の検査はすべて通っていた。**

**この課題で足すのは、その規則を機械で見る検査である。** 開発者から見える振る舞いは次のように変わる。

| 場面 | いま | 検査を入れた後 |
| --- | --- | --- |
| 指示書へ出た版の段落を書いたまま Pull Request を出す | どの検査も落ちない | `instruction-files-check` が落ち、ファイル・行・版数を指摘する |
| 指示書へ `@<パス>` を書く | 落ちない。参照先が毎回読み込まれる | 許可した先でなければ落ちる |
| 配布の Pull Request で退避を忘れる | 落ちない。次の版でも同じ状態が続く | **その Pull Request の中で**落ちる |

目的を 3 つにまとめると次になる。

- 出た版の判断が指示書へ残ったまま Pull Request を出すと、継続的統合の検査が落ちる
- 指示書へ即時読み込みの参照を足すと、許可した先でない限り同じ検査が落ちる
- 配布の Pull Request（版の節を `CHANGELOG.md` へ足すもの）で、退避を忘れるとその Pull Request の中で落ちる

## 実例: #551 の直前の状態は、既存の検査をすべて通った

**何を捕まえる検査なのかは、#551 の直前の状態で示せる。** `f56c90d9`（PR #552 のマージの直前の `develop`）の
`CLAUDE.md` は、この課題の検査に通すと落ちなければならない。

| 見るもの | `f56c90d9` の値 | 既存の検査 |
| --- | --- | --- |
| `CLAUDE.md` の行数 | 333 | 行数の検査（501 行以上で落とす）は**通る** |
| 行頭が `v<版数> で` の段落 | **7 個**（36 行目以降の `v10.0.0` 〜 `v10.5.1`） | 見ていない |
| そのときの `CHANGELOG.md` の最新の `ndf` の版 | `10.9.1` | 見ていない |
| 即時読み込みの参照（`@`） | `@AGENTS.md` と **`@docs/ndf-version-decisions.md`** | 見ていない |

7 個の段落はどれも `10.9.1` 以下の版、つまり**出た版**の段落である。2 つ目の `@` は出た版の
記録そのものを毎回読み込ませていた。**この 8 件をこの課題の検査が指摘できれば、同じ状態へ戻った
ときに継続的統合が止める。**

## 前提

- 前提 1: 版の判断を指示書へ書くのは `ndf` だけである（退避先の文書も `ndf` の版だけを扱う）
- 前提 2: 配布の Pull Request は、`CHANGELOG.md` へ版の節を足す Pull Request の中で退避もする。v10.12.0-dev.1 と v10.14.0-dev.1 の配布がこの形である
- 前提 3: 即時読み込みの一致範囲は Claude Code 2.1.274 で実測した範囲とする（設計文書の「実測」）

## 対象範囲

含む:

- 指示書 3 本の、出た版の段落と見出しの検出
- 版が決まる前に書く段落の書き出しの形を規約に足し、その形も検出する
- 指示書 3 本の、許可した先以外への即時読み込みの参照の検出
- 検査を継続的統合のジョブとして足すこと
- 規約（`CLAUDE.md` の「版ごとの判断の記録」）と、版を上げる手順（`docs/versioning-and-distribution.md`）へ検査を書くこと
- 規則と食い違う既存の 2 行（下の「範囲へ入れたもの」）
- 必須の検査の一覧（ruleset）へ `instruction-files-check` を入れること。**実行は実装 Pull Request のマージ後に進行側が行う**
- `docs/versioning-and-distribution.md` の「必須の検査 11 個」の数を、足した後の数へ直すこと

含まない:

| 扱わないもの | 理由 |
| --- | --- |
| 読み込みの量（トークン数）を閾値で落とすこと | 設計文書の決定 1。測るには CLI の認証と課金が要り、文字数で代用すると行数と同じ穴が残る |
| 版数を持たない書き出しで書いた段落の検出 | 機械では判別できない。規約で書き出しの形を決めて検出の側へ寄せる（決定 3） |
| `release` Skill の本文 | リポジトリに依存しない手順で、検査の名前を書けない（`check-skill-repo-assumptions.py`）。リポジトリ側の手順（`docs/versioning-and-distribution.md`）へ書く |
| 退避先の見出しが扱う版の範囲の検査 | 版の段落が無い版でも範囲を上げるかは規約が決めていない。#551 の範囲外として残る |
| `playwright-kit` と MCP プラグインの版 | 前提 1 |
| リリースタグとの突き合わせ | 決定 2。タグは `main` へ進めた後に打つため、配布の Pull Request の時点では無い |

### 範囲へ入れたもの

**規則と逆のことを書いている 2 行**（起票せず範囲内へ入れた）。どちらも「判断の理由は `CLAUDE.md` に置く」と書いており、
**#551 で決めた置き場所（`docs/ndf-version-decisions.md`）と食い違う**。検査を足す手順と同じ文書・同じ話題であり、
残すと検査が守らせる規則と手順書が逆を指す。

| 場所 | いまの記載 |
| --- | --- |
| `CHANGELOG.md` 7〜8 行目 | 「その版で決めた規約は `CLAUDE.md` の版ごとの段落に」 |
| `docs/versioning-and-distribution.md` の「バージョン更新時の手順」4 | 「判断の理由は `CLAUDE.md` の側に置く」 |

## 検査が落ちたときに何をするか

| 指摘 | 直し方 |
| --- | --- |
| 出た版の段落が指示書に残っている | その段落から次の見出しまでを `docs/ndf-version-decisions.md` の時系列の位置へ移す。移した先の見出しが扱う版の範囲も同じ操作で直す |
| 版が決まる前の段落が、配布を越えて残っている | 書き出しを `v<出す版> で` に直し、同じく退避先へ移す |
| 許可していない即時読み込みの参照がある | `@` を外す（リンクかコードスパンにする）。毎回読ませる必要があるなら、検査の許可の一覧へ理由とともに足す |
| 許可の一覧の指し先が無い | その許可を一覧から外す |

**現行版の判断をこれから指示書へ書くときは、書き出しを `v<直前の正式版> の次の版で` にする。**
版数はまとまりをマージするまで決まらないため、書く時点では直前の正式版しか書けない。この形で書いておくと、
配布で新しい版が出た時点で検査が拾い、退避の対象として指摘する。

## 受け入れ条件

### 出た版の段落

- [ ] AC1: `f56c90d9` の指示書 3 本と `CHANGELOG.md` を一時ディレクトリへ置き、検査を走らせる。終了コード 1 で終わる。`CLAUDE.md` の 38 / 88 / 113 / 137 / 172 / 207 / 252 行目の 7 件が版数つきで出る
- [ ] AC2: 現在の `develop` の根で検査を走らせると、終了コード 0 で終わる
- [ ] AC3: 最新の `ndf` の版が `10.14.0` のとき、行頭 `v10.14.0 で` の段落は終了コード 1 になる（最新と等しい版も出た版）
- [ ] AC4: 同じ条件で、行頭 `v10.15.0 で` と `v10.15.0-dev.1 で` の段落は指摘されない（まだ出ていない版）
- [ ] AC5: 行頭 `v9.8.0 で` のように **`CHANGELOG.md` に節の無い版でも**、最新の版以下なら指摘される（開発版までしか出なかった版の段落も過去の記録である）
- [ ] AC6: 見出し（`## v10.5.0 の判断` のように `#` で始まる行）が出た版の版数を含むと指摘される
- [ ] AC7: 行頭が `v10.14.0 で` の行が**コードブロックの中**にあるときは指摘されない
- [ ] AC8: 行の途中に出た版の版数がある行（`AGENTS.md` の `（v4.0.0 で Codex MCP サーバは廃止）`、`（v10.14.0）`）は指摘されない
- [ ] AC9: `playwright-kit` の節（`## [playwright-kit 2.0.3]`）は出た版の最大値の計算に入らない

### 版が決まる前の段落

- [ ] AC10: `CHANGELOG.md` の最新の `ndf` の版が `10.14.0` のとき、行頭 `v10.14.0 の次の版で` の段落は指摘されない
- [ ] AC11: 同じ段落を残したまま `CHANGELOG.md` へ `## [ndf 10.15.0]` の節を足すと、終了コード 1 になり、その段落が指摘される

### 即時読み込みの参照

- [ ] AC12: `f56c90d9` の状態（AC1）で、`CLAUDE.md` 36 行目の `@docs/ndf-version-decisions.md` が許可していない参照として指摘される
- [ ] AC13: `**@AGENTS.md**`（強調の中）と行頭・空白の直後の `@AGENTS.md` は、許可した先なので指摘されない
- [ ] AC14: 許可していない先（`@docs/foo.md`）を空白の直後・強調の中に書くと、それぞれ指摘される
- [ ] AC15: コードスパン（`` `npm install -g @openai/codex` ``）とコードブロック（`KIRO.md` の `<@U0123456789>`）の中の `@` は指摘されない
- [ ] AC16: メールアドレスの形（`user@example.com`）は指摘されない
- [ ] AC17: 参照の直後に句読点が続く `@docs/foo.md。` は、名前 `docs/foo.md` の参照として指摘される
- [ ] AC18: 許可の一覧に載せた先のファイルが存在しないと、許可そのものが古いとして終了コード 1 になる

### 読み取れないとき

- [ ] AC19: `CHANGELOG.md` が無い、または `## [ndf X.Y.Z]` の節が 1 つも読めないとき、終了コード 2 で終わり、理由が標準エラーへ出る
- [ ] AC20: 指示書 3 本のいずれかが無いとき、終了コード 2 で終わる

### 継続的統合と手順

- [ ] AC21: `.github/workflows/runtime-plugin-validate.yml` に検査のジョブがあり、Pull Request では絞り込まずに起動する。push の絞り込みに `CHANGELOG.md` が入る
- [ ] AC22: この課題の実装の Pull Request で、そのジョブが pass する
- [ ] AC23: `CLAUDE.md` の「版ごとの判断の記録」に、版が決まる前の書き出しの形と、検査のコマンドが書かれている
- [ ] AC24: `docs/versioning-and-distribution.md` の「バージョン更新時の手順」に検査のコマンドがある。手順 4 は判断の理由の置き場所を `docs/ndf-version-decisions.md` と書く
- [ ] AC25: 同じ文書の「必須の検査 11 個」の記載が、`instruction-files-check` を足した後の数になっている
- [ ] AC26: `CHANGELOG.md` の冒頭が、判断の理由の置き場所を `docs/ndf-version-decisions.md` と書く

### 退行しないこと

- [ ] AC27: 既存の検査 5 本が終了コード 0 のまま。対象は `check-doc-line-limit.py` / `check-markdown-links.py` / `check-doc-staleness.py` / `check-skill-repo-assumptions.py` / `check-skill-frontmatter.py`
- [ ] AC28: `uv run --with pytest pytest scripts/tests -q` で、足したテストを含めて失敗が増えない

## 非機能の条件

| 大項目 | 条件 |
| --- | --- |
| 運用・保守性 | 指摘は `ERROR: <ファイル>:<行>: <理由>`（行を持たない指摘は `ERROR: <理由>`）の 1 行で、直し方（退避先・許可の一覧）を含む。外部への通信・CLI の認証を要さない |
| システム環境 | Python 3 の標準ライブラリだけで動く。`actions/checkout` の既定（浅い clone・タグなし）で動く |

## 影響

| 対象 | 影響 |
| --- | --- |
| 公開インタフェース | 変わらない。配布物（`plugins/`）は触らない |
| 既存の振る舞い | 継続的統合に検査が 1 つ増え、必須の検査になる。落ちるとマージできない |
| 規約 | 版が決まる前に指示書へ書く段落の書き出しが決まる |

## 検証手段

| 項目 | 手段 |
| --- | --- |
| 検査 | `python3 scripts/check-instruction-files.py --root .` |
| テスト | `uv run --with pytest pytest scripts/tests/test_check_instruction_files.py -q` |
| 過去の状態での再現（AC1 / AC12） | `git archive f56c90d9 CLAUDE.md AGENTS.md KIRO.md CHANGELOG.md` を一時ディレクトリへ展開し、`--root` で渡す |
| 継続的統合 | 実装の Pull Request の checks でジョブの結果を見る |

## 境界

| 区分 | 内容 |
| --- | --- |
| 常に行う | 既存の検査とテストの実行、既存の `check-*.py` の書き方（終了コード 0/1/2、`--root`、docstring に理由）に合わせる |
| 確認してから行う | 必須の検査の一覧（ruleset）の変更。利用者が入れると決めたため、実行は実装 Pull Request のマージ後に進行側が行う |
| 行わない | `release` Skill の本文の変更、退避先の文書の中身の変更、読み込みの量の測定 |

## 未決

未決の項目は無い。必須の検査へ入れるかは利用者が決めた（入れる）。

## 用語

| 用語 | この文書での意味 |
| --- | --- |
| 指示書 | 全セッションが毎回読むリポジトリ直下の `CLAUDE.md` / `AGENTS.md` / `KIRO.md` の 3 本 |
| 出た版 | `CHANGELOG.md` に `## [ndf X.Y.Z]` の節がある版。**その最大値以下の版**を出た版として扱う |
| 版の段落 | 行頭が `v<版数> で` で始まる段落。退避先（`docs/ndf-version-decisions.md`）と #551 以前の `CLAUDE.md` が使う書き出し |
| 即時読み込みの参照 | コードの外に書いた `@<パス>`。Claude Code は参照先の内容をそのまま読み込む |

## 依頼（原文）

> **「開発の指示書には現行版で決めたことだけを残す」という規則を、機械で見ていない。**
>
> （中略）
>
> | 何を見るか | 判定の仕方 |
> | --- | --- |
> | 出た版の見出しが指示書に残っていないか | 配布済みの版数の一覧（変更履歴の文書またはタグ）と突き合わせる |
> | 即時読み込みの記法が使われていないか | 指示書の中の `@` 参照を数え、許可した先だけに限る |
> | 指示書の分量そのもの | 行数ではなく**読み込みの量**で見る（分量の基準は文書の種類で変わる） |
>
> **分量だけで見ない。** 今回の 333 行は基準内であり、行数では捕まえられなかった。

（#554 本文の「何を見つけたか」「考えられる見方」から引用。全文は課題にある）
