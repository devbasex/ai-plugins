# 実機での確認（#436 の受け入れ条件 E4）

**新しい `cross-refactoring` を PR #448（運用モードの新設）に対して 1 度回した実測である。**

- 実行日: 2026-09-06
- 対象: Pull Request #448 / ブランチ `feat/issue-423-operation-mode`
- 範囲: `plugins/ndf/skills/development-workflow/scripts` と `.../tests`
- 着手前のテスト: `uv run --with pytest pytest plugins/ndf/skills/development-workflow/tests -q`
- 起動: `--workflow-step`（`development-workflow` の 1 工程として）

## 設計の決定が実機で働いたか

| 決定 | 実機での確認 | 結果 |
| --- | --- | --- |
| 5. `--scope` にテストの置き場所が無ければ**止める** | 範囲に `scripts` だけを渡した | **止まった。**「テスト整備ラウンドは現状固定テストを `--scope` の中へ足すため、含めないとその項目は必ず失敗します」と述べて終了 |
| 4. テストの整備はラウンドである | 最初のラウンドを開いた | **`ROUND_KIND=test` / `PROPOSE_PHASE=propose-tests`**。構造改善より前に来た |
| 9. テスト整備の提案に新しい語彙を作らない | 提案の中身 | `case` に `boundary` / `branch` / `error`、`level` に `unit`。**既存 3 本の参照から採った語彙が使われた** |
| — | 重複排除の鍵（`target` + `case`） | 提案 11 件 → **統合 10 件**（1 件が重複として畳まれた） |
| — | 採用上限（既定 5） | **採用 5 件 / 見送り 5 件** |
| 1. 適用ラウンドは**書き換えるファイルの一致**で分ける | 採用 5 件の割り当て | **2 つの群に分かれた。** 群 1 は R1-001〜004（4 つとも別のテストファイル）、群 2 は R1-005（**R1-001 と同じ `test_operation_mode.py` を書き換えるため別の群**） |
| — | 輪番が適用ラウンドごとに進む | 群 1 は codex、**群 2 は agy** |
| 2. コミットは適用ラウンドごとに 1 つ | 群 1 の適用 | **4 項目が 1 コミット**（80 行）。従来なら 4 コミット |
| 6. 改修計画は Pull Request のコメント 1 件 | 適用の後 | **コメントとして作られた**（ファイルではない）。https://github.com/devbasex/ai-plugins/pull/448#issuecomment-5562509486 |
| 3. 検証はテストの結果で決まる | 群 1 の検証 | **2 CLI のレビューを起動せず、テストの合否で通過**（`exit_code: 0`） |
| 6-b. 外へ出す文章 | 検証の出力 | **`<ファイル>#<シンボル>` が併記**され、**改修計画の URL が生の URL**で入った |

## 実測値

| 項目 | 値 |
| --- | --- |
| 提案（3 者の合計） | 11 件（codex 3 / agy 5 / kiro 3） |
| 統合後 | 10 件 |
| 採用 / 見送り | 5 件 / 5 件 |
| 適用ラウンドの数 | **2**（重なり方が決めた） |
| 群 1 の適用 | 4 項目 / 1 コミット / 80 行 / **120 秒** |
| 群 1 の検証 | **テスト 1 回**（`exit_code: 0`）。**修正ラウンドは 0 回** |
| 適用したコミット | `fb2726d`（群 1、80 行）と `c0d9087`（群 2） |
| 足された現状固定テスト | **12 件**（全体は 2563 → 2575 件） |
| **レビューの起動** | **0 回**（従来は 1 ラウンドあたり 2 者・200〜860 秒） |

## 変更前との比較

**PR #435 で回した旧いフロー**（4 ラウンド）と比べる。

| 指標 | 旧（PR #435） | 新（PR #448、テスト整備ラウンド 1 回） |
| --- | --- | --- |
| レビューの起動 | 4 ラウンド × 2 者 = **8 回** | **0 回** |
| レビューの所要 | **1843 秒** | 0 秒 |
| 修正ラウンド | 0 回 | 0 回 |
| 取り消し | **18 項目のうち 9 件** | 0 件（群 1 は検証を通過） |

**取り消しの主因だった「範囲外への変更」は、`--scope` の関門が実行前に止めた。**

## 群 2 も完走した

**進行側は群 2 の適用を中断させたと判断したが、誤りだった。** 起動し直す際に自分の
プロセスグループへシグナルを送ってしまい、その時点で止まったと見なした。

実際には**群 2（R1-005）も適用され、コミット `c0d9087` が作られていた**。適用ラウンドは
2 つとも通り、テスト整備ラウンド 1 で採用した 5 項目すべてが差分に入っている。

| 群 | 担当 | 項目 | コミット |
| --- | --- | --- | --- |
| 1 | codex | R1-001〜004 | `fb2726d`（80 行） |
| 2 | agy | R1-005 | `c0d9087` |

**輪番が適用ラウンドごとに進むことも、これで確かめられた。**

## 未確認のまま残ること

| 項目 | 内容 |
| --- | --- |
| 修正ラウンド | **テストが落ちる場面に当たらなかった。** 検証は 1 回で通った |
| `--ci-check` | 指定せずに実行したため、継続的統合での代替は未確認 |
| 最終ゲート（Step 7） | 提案ラウンドまで進んでいないため未確認 |
| テスト整備ラウンドの 2 巡目 | 1 巡目で終えたため、上限 2 の効き方は未確認 |
