# #712 / #713: 同じ上位原因を持つ課題群を束ねる判定と、構造の判断の担い手

設計は [issue-712-713-design.md](issue-712-713-design.md) にある。この文書は「何を満たすか」だけを扱う。

**2 つの課題は 1 本の Pull Request で直す。** #713 は #712 で決まる判定の形を前提にし、
どちらも `plugins/ndf/skills/issue-upkeep/SKILL.md` の「既存の Skill との境界」を触る。分けると
同じ節を 2 本の Pull Request が同時に書き換える。

## 依頼の原文

> /ndf:development-workflow https://github.com/devbasex/ai-plugins/issues/712 https://github.com/devbasex/ai-plugins/issues/713
>
> 今後のissue解決の進行に関わるので、先に直してしまいましょう。

**解釈。** 2 件を工程に乗せて直す。「先に」は着手の順序であり、工程を省くことではない。

## 目的

- 棚卸が同じ上位原因を持つ課題群を見つけたとき、**その群を記録して 1 つの根本修正へ向ける
  行き先がある**
- 4 つの Skill（`out-of-scope` / `problem-solving` / `retrospective` / `issue-upkeep`）の
  どれから読み始めても、**蓄積した課題群の共通原因を扱う Skill へたどり着ける**
- 2026-09-16 の棚卸で見つかった 4 群のうち 2 群以上が、実際に束ねられている

## 対象範囲

含む:

- `plugins/ndf/skills/issue-upkeep/SKILL.md` の判定の表・用語の表・「扱う 3 つ」の節・
  段 2A の控える項目・段 2B の突き合わせの表・段 2B の「起点が同じことと同じ課題である
  ことを分ける」の注記・自動で反映してよい変更の表・完了報告・既存の Skill との境界
- `plugins/ndf/skills/issue-upkeep/references/` に、束ねる判断の必要条件と上位の課題の
  書き方を置く新しい参照
- `plugins/ndf/skills/issue-upkeep/references/milestones.md` の、束ねた上位の課題を
  割り当てる箇所
- `plugins/ndf/skills/out-of-scope/SKILL.md` の「蓄積した課題との境界」
- `plugins/ndf/skills/problem-solving/SKILL.md` の「上流で直す」と関連
- `plugins/ndf/skills/retrospective/SKILL.md` の「蓄積した課題を手入れする」
- `plugins/ndf/skills/issue-upkeep/tests/test_issue_upkeep_layout.py`
- 上の 4 群のうち 2 群以上を実際に束ねた結果（上位の課題の起票と、元の課題への結び付け）

含まない:

| 扱わないもの | 理由 |
| --- | --- |
| 4 群すべてを束ねること | 完了条件は 2 群以上である。残りは次の棚卸が同じ手順で扱う |
| 束ねた上位の課題そのものを直すこと | 束ねるのは判定であって、修正は各マイルストーンの着手で行う |
| 元の課題を閉じること | 下の AC10 で「閉じない」と決める。閉じる判断は上位が直った後の棚卸が既存の判定で行う |
| マイルストーンの新設・並べ替え | `references/milestones.md` の既存の規則で決まる。足すのは上位の課題を入れる先を決める 1 行だけである |
| `out-of-scope` の 3 択を 4 択へ増やすこと | 発見の瞬間には群が見えない（#713 の分析）。入口は 3 択のままにする |
| `retrospective` に群の発見を担わせること | 振り返りは 1 回の変更を見るため、群は見えない |
| `CHANGELOG.md` と版数 | 配布の工程が書く |
| クラス図・データ構造の図 | 変えるのは文書の節と表であり、型を持たない |

## 前提

- **前提 1: 判定の名前は「束ねる」とする。** #712 / #713 の本文がこの語を使っており、
  別の語を当てると課題の本文と食い違う
- **前提 2: 判定は 8 つになる。** 既存の 7 つはいずれも残す。どれも別の意味を持ち、
  統合すると後から検索で区別できない（`no-work.md` が「閉じてよい」と「やらない」で
  同じ理由を挙げている）
- **前提 3: 上位の課題の起票は、承認を得てから行う。** 外部から見える場所への書き込みで
  あり、`out-of-scope` も起票の前に同意を取る

## 受け入れ条件（#712: 束ねる判定）

判定の表:

- [ ] AC1: `SKILL.md` の「| 判定 | 選ぶ条件 | 段 3 での対応 |」の表が 8 行になり、並びが
  `そのまま` / `追記が要る` / `書き直しが要る` / `閉じてよい` / `やらない` / `重複` /
  `束ねる` / `要判断` である。`束ねる` の行に選ぶ条件と段 3 での対応が書かれている
- [ ] AC2: `束ねる` の行から、必要条件を持つ参照（`references/` 配下）へのリンクがある
- [ ] AC3: 「自動で反映してよい変更と、返す変更」の表に束ねる操作の行があり、
  **一括で提示して承認を得てから行う**側に分類されている
- [ ] AC4: 「扱う 3 つ」の節が「扱う 4 つ」になり、4 つ目の行が課題群を束ねる判断を示す。
  節末の参照への案内文が `references/grouping.md` を指す。用語の表の「判定」の定義が
  **8 つの区分**になっている

段 2A:

- [ ] AC5: 段 2A の控える項目に「この課題が指す上位原因」の行があり、書き方
  （規則・層・届け先のいずれかで「何が無いか」を 1 行）が示されている。**段 2A では
  群を決めない**ことと、その理由（件数で割られており他の課題の控えが見えない）が
  書かれている

段 2B:

- [ ] AC6: 段 2B の「突き合わせるもの」の表に「同じ原因を持つ課題群」の行があり、既存の
  「同じ前提の変化を受けた課題群」の行が残っている。2 つの違い（原因を束ねるのか、
  書き換えの言い回しを揃えるのか）が本文で 1 文以上示されている
- [ ] AC7: 段 2B の「起点が同じことと同じ課題であることを分ける」の注記が、束ねるとの
  分岐を持つ。分岐の軸（件数が 3 件以上か、上位を直した後に個別の作業が残るか）で、
  同じ起点を持つ組が重複へ倒れるか束ねるへ倒れるかが読み取れる

必要条件と書き方の参照:

- [ ] AC8: 参照が「重複」との境目を示す。**上位を直しても各課題に個別の作業が残るなら束ねる、
  残らないなら重複である**ことが、判定できる形で書かれている
- [ ] AC9: 参照が、束ねてよい必要条件を**すべて満たすものだけを候補にする**形で列挙し、
  確かめ方と、欠けたときの行き先（既存の 7 判定のどれか）を列で持つ。`no-work.md` の
  「2 つの必要条件」と同じ 3 列（条件 / 確かめ方 / 欠けたときの行き先）にする
- [ ] AC10: 参照が「**元の課題を閉じない**」ことと、その理由（個別の観測が失われる／上位を
  直しても個別の適用が残る）を示す。元の課題へ足す 1 行の書式が例で示されている
- [ ] AC11: 参照が、上位の課題の本文の項目を列挙する。項目に**元の課題の番号と、その課題が
  示す観測の 1 行要約の表**が含まれる（再現手順の本体は元の課題に残す）
- [ ] AC12: 参照が、**1 件が複数の群へ属してよい**ことと、その扱い（リンクを群の数だけ足す。
  上位の課題どうしが同じ箇所を直すなら 1 つの群である）を示す。**やり直しで起票を飛ばすのは、
  既にある 1 行が指す上位の課題の「何が無いか」がこの群の「無いもの」と一致するときだけ**で
  あることが書かれている
- [ ] AC13: 参照が、**束ねた結果が大きすぎて着手できない形を避ける歯止め**を持つ。歯止めは
  「上位の課題の受け入れ条件が作るもの 1 つで書けること」を必要条件として含み、書けない
  ときの行き先が示されている
- [ ] AC14: 参照が、**上位の課題の重要度は群の中で最も高いものを採る**と定め、マイルストーンは
  `milestones.md` で決まると示す。`milestones.md` に足す行が、**重要度が「高い」ときは既存の表の
  とおり直近のまとまりへ入れる**ことを先に示し、**主題で重要度が決まるときだけ、群の課題が既に
  属するマイルストーンのうち最も早いものを採る**と書かれている。群のどの課題もマイルストーンを
  持たないときは既存の表で決まる。**元の課題のマイルストーンは動かさない**ことと、その理由
  （他の課題の着手の順序を変える判断にあたる）が書かれている

起票の例外:

- [ ] AC15: 「**「手入れ」は起票を含まない**」の段落に例外が書かれ、例外が束ねる判定の作る
  上位の課題 1 つだけであることと、その理由（`out-of-scope` は発見の瞬間しか見ないため
  群を作れない）が示されている。用語の表の「手入れ」の定義もこれと食い違わない

完了報告:

- [ ] AC16: 「完了報告」の表に**束ねた群の数**を書く項目があり、「判定の内訳」の行が
  8 つの判定を指している

検査:

- [ ] AC17: `test_issue_upkeep_layout.py` の判定の並びが 8 つになり、`束ねる` を含む順序で
  検査している
- [ ] AC18: `test_issue_upkeep_layout.py` が、束ねる判定の行から参照へたどれることを検査する
- [ ] AC19: `test_issue_upkeep_layout.py` が、参照の必要条件の節・元の課題を閉じないこと・
  歯止め・複数の群への所属を検査する
- [ ] AC20: `test_issue_upkeep_layout.py` が、段 2B の表に「同じ原因を持つ課題群」の行が
  あることを検査する
- [ ] AC21: `uv run --with pytest pytest plugins/ndf/skills/issue-upkeep/tests -q` が通る

実地:

- [ ] AC22: 2026-09-16 の棚卸で見つかった 4 群のうち **2 群以上**について、上位の課題が
  起票されている。上位の課題の本文が AC11 の項目を満たす
- [ ] AC23: 束ねた群の元の課題すべてに、上位の課題を指す 1 行が本文へ入っている（AC10 の書式）
- [ ] AC24: 束ねた結果（群の名前・上位の課題の番号・元の課題の番号・元の課題の扱い）が
  この変更の Pull Request の本文に表で残っている

## 受け入れ条件（#713: 構造の判断の担い手）

- [ ] AC25: 判断（価値 / 構造）× 見る対象（発見の瞬間 / 蓄積した課題）の 2 × 2 の表が、
  少なくとも 1 つの Skill に書かれている。構造 × 蓄積した課題の升が `issue-upkeep` の
  「束ねる」を指している
- [ ] AC26: `issue-upkeep` の「既存の Skill との境界」が、価値の判断と構造の判断の両方に
  ついて担い手を示している
- [ ] AC27: `out-of-scope` の「蓄積した課題との境界」に、「蓄積した課題には及ばない」の
  及ぶ先が `issue-upkeep` であると書かれている
- [ ] AC28: `problem-solving` に、「上流で直す」（1 件の不具合の上流）と棚卸の「束ねる」
  （複数の課題の共通原因）の違いが 1 文以上で書かれ、`issue-upkeep` を指すリンクがある
- [ ] AC29: `retrospective` に、**群の発見を担わない**ことと、その理由（振り返りは 1 回の
  変更を見るため群が見えない）が書かれている
- [ ] AC30: 4 つの Skill のどれから読み始めても `issue-upkeep` へたどれる。
  `test_issue_upkeep_layout.py` の「この Skill を指す」検査の対象が
  `retrospective` / `release` / `out-of-scope` / `problem-solving` の 4 つになっている

**検査の対象と、目的の 4 つの Skill は別の集合である。** 目的が挙げる 4 つは
`out-of-scope` / `problem-solving` / `retrospective` / `issue-upkeep` であり、
`issue-upkeep` は指される側なので検査の対象にならない。既存の対象にある `release` は
配布の工程から棚卸を呼ぶ経路であり、この変更では外さない。結果として対象は
`problem-solving` を足した 4 つになる。

## 検証手段

| 何を確かめるか | コマンド |
| --- | --- |
| 判定の並び・参照・境界の記述 | `uv run --with pytest pytest plugins/ndf/skills/issue-upkeep/tests -q` |
| 他の Skill のテストを壊していないこと | `uv run --with pytest pytest scripts/tests plugins/ndf -q` |
| frontmatter の規約 | `python3 scripts/check-skill-frontmatter.py` |
| プラグイン定義 | `claude plugin validate .` |
| 上位の課題と元の課題の結び付き（AC22〜AC24） | `gh issue view <番号>` の出力を Pull Request の本文へ写す |

AC22〜AC24 は自動テストにできない（GitHub の状態であり、リポジトリの中に無い）。
**手動の確認手順**は、束ねた群ごとに `gh issue view <上位の番号>` と、元の課題それぞれの
`gh issue view <番号> --json body --jq '.body' | grep '#<上位の番号>'` を実行し、出力を
Pull Request の本文へ残すこととする。

## プロジェクト構造

| 置く場所 | 何を |
| --- | --- |
| `plugins/ndf/skills/issue-upkeep/SKILL.md` | 判定の表・段 2B・境界・完了報告 |
| `plugins/ndf/skills/issue-upkeep/references/` | 束ねる判断の必要条件と上位の課題の書き方 |
| `plugins/ndf/skills/issue-upkeep/tests/` | 構成と配線の検査 |
| `issues/` | この仕様・設計・実装計画 |

**置いてはいけない場所。** `docs/` へは置かない（確定仕様化の工程が行う）。Serena memory は
使わない（`CLAUDE.md`）。判定の基準を `SKILL.md` の外へ写さない（`development-workflow` が
「判定基準を持つのはこの Skill だけ」としたのと同じ理由で、判定の並びの正本は
`SKILL.md` の表 1 箇所である）。

## コーディング規約

- 文書は `markdown-writing` に従う。**変更の経緯を本文に残さない**
- パスの直後は半角スペースを置く
- frontmatter は `plugins/ndf/skills/AUTHORING.md` の規約に従い、
  `python3 scripts/check-skill-frontmatter.py` で検査する
- テストは既存の `test_issue_upkeep_layout.py` の書き方（docstring に理由、
  `SKILL.md` の文字列を直接照合）に合わせる

## テスト戦略

| 階層 | 何を担保するか |
| --- | --- |
| 構成の検査（既存の `test_issue_upkeep_layout.py`） | 判定の並びが 3 箇所で揃うこと、参照が存在すること、呼び出し元が指すこと |
| 手動確認 | AC22〜AC24（GitHub 上の状態） |

**新しいテストファイルを作らない。** 既存の 1 本が同じ Skill の構成を検査している。

## 境界

```text
常に行う      … 既存テストの実行、frontmatter の検査、変更した Skill の語の統一
確認してから行う … 上位の課題の起票、元の課題の本文の書き換え、マイルストーンの設定
行わない      … 元の課題を閉じること、4 群すべてを束ねること、out-of-scope の 3 択を増やすこと
```
