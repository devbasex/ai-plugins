# NDF の実験版の台帳

**実験版は、呼んだときだけ働く仕組みを、課題の順番を待たずにその場で入れて実践で試すものである。**
置き場は `plugins/ndf/scripts/experimental/`、規則は `AGENTS.md` の「安定版と実験版」にある。

足したら 1 行を書き、使うたびに「使った結果」へ足す。昇格か廃止を決めたら「行き先」を埋める。

| 実験版 | 何を試すか | 入れた日・PR | 使った結果 | 行き先 |
| --- | --- | --- | --- | --- |
| `resume.py` | 再開した会話が、ラッパーの判定・セッションの始まり・前のセッションの終わり・版を 1 回で得る | 2026-09-25・`exp/resume-and-issue-body` | 2026-09-25 03:4x: 3〜4 往復の調べを 1 回にした。手で起動したセッションと、前のセッションの `no-mark` を読めた。2026-09-25 04:26: 目印から自動で起動したセッション 2 と、前のセッションの終わり `mark`（3626 秒）・版 10.17.13 の一致を 1 回で読み、#980（アナウンスどおり自動で切り替わった）と #1000（/goal 未達でも切り替わった）を実機で確かめた | 未定（relay.py の副命令へ。#1017） |
| `issue-body.py` | 課題の本文の書き直しを、読み直して一致まで確かめる。書く前に、手元にだけあって GitHub から読めないパスを本文が指していないかを見る | 2026-09-25・`exp/resume-and-issue-body` | 2026-09-25: #1017 を書き直して一致を確かめた。2026-09-25 13:5x: #1111 の本文がコミット前の `issues/` の要求文書を指すだけだった形を、書き込む前に止めた（終了コード 1） | 未定（本文を書き直す Skill の案内へ。#1017） |
| `phase_cost.py` | supervise.py のステップの種類ごとの費用・所要・往復と、Agent の記録の層・フェーズごとの実作業・ピーク使用量を 1 回で並べる | 2026-09-25・`fix/issue-768-phase-names` | 2026-09-25: プラン 11 件（ステップ 95 件）と conductor のセッション 3 件を測り、#773 の候補を出した | 未定（`skill-stats --agents` のステップの表へ。#773） |
| `review-terms-count.py` | 設計 PR の返信を除いたインラインの指摘を、語・定義と食い違いの語の並びで数え、ラウンド数を添える。語の定めが指摘を減らしたかを測る | 2026-09-25・`feat/issue-1111-m26-s11` | 2026-09-25: #742 を数えた。指摘 97 件・語・定義 32 件・食い違い 45 件（基準の値と一致）。次の設計 PR で数えて比べる | 未定 |
| `mvv-gate.py` | `pace: fast` のゲート 1・2 を、利用者が承認した MVV に照らして最小構成の `claude -p` が判定する（従うなら承認ゲートを省く。従わない・判定できない・レッドラインなら利用者の承認へ戻る） | 2026-09-25・`exp/mvv-gate` | 2026-09-25: 本番 10.17.19 のゲート 2 で unknown（$0.06・20 秒）。材料の食い違いと効果の実測の欠けを挙げ、利用者の承認へ戻した | 本体へ移した（`plugins/ndf/scripts/mvv-gate.py`。`pace: fast` の承認ゲートの判定として既定の手順から呼ぶ） |
| `deps-trial.py` | #1142 の決定 17 を L0 に入れる前に、`require()` の試作（uv の環境へ起動し直す・uv が無ければ入れる）と githubkit の ETag での読み直しを確かめる | 2026-09-26・`exp/deps-trial` | 2026-09-26: 3 つとも成り立った。ETag での読み直しは上限に数えられない（最初の 200 だけが 1 回。`/rate_limit` の used は応答の `X-Ratelimit-Used` と食い違うため物差しにしない）。4 ランタイム（Claude Code・Codex・Kiro・agy）から起動し直して import できた。uv の無いコンテナで curl 1.24 秒・pip 1.61 秒で入れて続き、2 回目 0.07 秒。ネットワークが無ければ終了コード 3 で止まる | L0（`lib/deps.py` と `lib/gh_parts.py`）へ移す。移したら消す |
| `runner-trial.py` | #1142 の不足 i の設計の前に、プランの実行とキューを置き換える候補（LangGraph の SqliteSaver・Burr の SQLitePersister・DBOS Transact の SQLite）で、`supervise.py new impl` の雛形（ステップを sleep と決めた終了コードの偽物にしたもの）・キュー・kill -9 からの再開・進捗ログと `supervise.py wait` を保てるかを確かめる | 2026-09-26・`exp/runner-trial` | 2026-09-26: 3 候補ともサーバーを立てず（プロセスの待ち受けのポート 0）SQLite のファイルだけで動き、確かめた 5 つが成り立った（`check <候補>` の結果がすべて ok）。表せるか: `on_fail`・judge の選択・上限 20・承認ゲート（止まり、承認の無い打ち直しは同じ所で止まったまま、承認で続く）を 5 つのシナリオで supervise.py の規則どおりの経路で流した。LangGraph の `interrupt()` は続けるときノードを頭から流し直すため、止める前の副作用はノードの外に置く。`recursion_limit` は invoke ごとに数え直すため、上限は状態で数える。Burr は遷移を `when(cur=...)` で行き先ごとに並べ、承認ゲートは `halt_before` で止める。DBOS は承認ゲートで `DBOS.recv` の待ちのままプロセスを抜け（`os._exit`）、打ち直しの `DBOS.launch()` が記録から続ける（落ちた後は状態が ENQUEUED に見える）。キュー: 6 本（ステージ 4 本 → 2 本）を同時 2 本・GraphQL の枠 1 本で流し、同時は最大 2 本・GraphQL は最大 1 本（枠の待ちが 1〜8 回起きた）で、後のステージは前のステージがすべて終わってから流れ、前のステージの 1 本が止まると後のステージは流れなかった。キューを持つのは DBOS だけ（`register_queue` の `worker_concurrency`。資源のタグは別のキューの `concurrency`）。LangGraph と Burr はキューを持たないため、子プロセスと flock の枠で自前にした（約 60 行）。6 本の所要は LangGraph 6.9 秒・Burr 6.0 秒・DBOS 12.4 秒（DBOS はキューの問い合わせの間隔 0.1 秒の分だけ遅い）。再開: 3 候補とも kill -9 のときに流れていたステップ（test-limited）から流し直し、済んだステップは流し直さなかった。費用（uv 0.12.19・キャッシュの冷えた状態。LangGraph / Burr / DBOS の順）: 解決 0.28 / 0.45 / 0.21 秒、導入 0.32 / 0.20 / 0.45 秒（キャッシュが温まって 0.03 / 0.01 / 0.01 秒）、大きさ 54.9 / 20.2 / 60.6 MB、依存 41 / 2 / 11 件（LangGraph は langchain-core・langsmith・pydantic-core を、DBOS は SQLite でも SQLAlchemy・psycopg-binary を入れる）、import 冷えて 0.82 / 0.06 / 0.78 秒・温まって 0.26 / 0.04 / 0.24 秒、起動し直しを含むエントリポイント（温まって）0.31 / 0.08 / 0.31 秒。進捗ログと wait: 進捗ログの行（step・attention）はライブラリでなく NDF の部品がステップの中で書き、今の `supervise.py wait` は手を入れずに attention で 20、done で 0 を返した | 未定（プランの実行とキューの設計へ。#1142） |

## 実験版の仕組みそのものの直し

| 日付 | 何が起きたか | 直したこと |
| --- | --- | --- |
| 2026-09-25 | 開発版 10.17.13-dev.1 のリリースで、境界のテストが README の更新案内に書いた置き場のパスに当たって落ちた。修正のステップは表記を言い換えて通した | テストが見る範囲を、既定で動くもの（コード・hook・設定と Skill・エージェントの本文）に絞った。Skill の本文に書けば落ち、README なら通ることを確かめた |
