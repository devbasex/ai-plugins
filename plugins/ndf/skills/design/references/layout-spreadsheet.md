# 体裁設計: 表計算

**読み手へ渡す文書を作るとき、出力の形が表計算なら読む。**

共通規則は [layout-common.md](layout-common.md) にある。このファイルでは表計算固有の項目だけを
決める。

## 決める項目

| 項目 | 決めること |
| --- | --- |
| シートの分け方 | 何を 1 シートにするか |
| 列の並び | 左から読む順。**識別子・分類・値の順が既定** |
| 見出しの固定 | 先頭行と先頭列を固定するか |
| 数値の書式 | 桁区切り・小数点・単位・通貨 |
| 色 | 地・文字・強調の 3 つ。**条件付きの書式を使うなら条件も** |
| 式か値か | **式のまま渡すか、値へ固定するか**（変換で式が失われる経路がある） |
| 図表の種類 | 埋め込むグラフの種類 |
| 生成の経路 | 変換器か、AI エージェントか |

## 関連

- `release` の `form-spreadsheet.md` — 決めた値を生成へ渡す手順
