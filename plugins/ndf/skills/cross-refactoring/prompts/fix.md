# 検証の失敗の修正（cross-refactoring / $RF_RUNTIME）

あなたは **$RF_RUNTIME**（モデル: $RF_MODEL）として、**範囲テスト（または危険の印で
走らせた全体のテストのうち変更が原因で落ちたテスト）が落ちた項目を直します**。同じテストのコマンドを共有した項目は、どの項目が落としたかまでは
分かっていません。まとめて 1 つの修正の対象です。

## 必須コンテキスト

- リポジトリ: $RF_REPO / Pull Request #$RF_PR
- 作業ディレクトリ: `$RF_WORKDIR`（**ここでだけ作業する**）
- ブランチ: `$RF_HEAD_BRANCH`

## 手順書

$RF_SKILL_BLOCK

## 直す項目

```json
$RF_ITEMS
```

- `test_command` が落ちたコマンド、`test_log` が進行側で走らせたときの出力です
- `fix_count` はこれまでの修正の回数です。上限に達するか想定最大時間の残りが尽きると、
  その項目は取り消されます

## やること

1. `test_log` を読み、`test_command` を実行して**どのテストがなぜ落ちているか**を読む
2. **振る舞いを元へ戻す方向で**直す。テストの側を書き換えて通すのではありません
   （**テストを緩めた時点で、振る舞い不変を示す手段が消えます**）
3. 直してから `test_command` をもう 1 回実行し、**項目ごとに 1 コミット**へまとめる

**振る舞いを変えないと直せない**と判断したときは、直さずに終えてください。無理に
通すより、取り消されるほうが安全です。

## コミットの規約

```text
Fix: 検証の失敗を直す — src/foo/bar.py#BarService.handle

（何をどう直したか）

Item-Id: I-002
Impl-Runtime: $RF_RUNTIME
Impl-Model: $RF_MODEL
```

- `Item-Id` は**上の直す項目のどれか**。それ以外の `Item-Id`・トレーラーの欠け・
  範囲外の変更・期待値の変更が 1 件でもあると、**この修正の範囲ごと取り消されます**
- **3 つのトレーラーはメッセージの最後の段落に置く**

## 作業の進み具合を残す

**作業段階が進むたびに `$RF_STEM-progress.log` へ 1 行追記してください**
（`start` / `edit` / `test` / `commit` / `done`）。

## 守ること

- **push しない。** 公開するのは進行側だけで、**検証を通した後**に行います
- **`git push --force` と `--no-verify` を使わない**
- コミット時のフックが生成物の同期を求めても同期しない。フックが原因でコミットできない
  ときは `git -c core.hooksPath=/dev/null commit ...` で**そのコミットだけ**フックを外す
- 直した内容は必ずコミットする。作業ツリーに置いたままの変更は捨てられます
- 作業ディレクトリの外と、対象範囲（`$RF_SCOPE`）の外を変更しない
- 生成物・配布物の同期をしない
- 失敗と関係の無い箇所を「ついでに」直さない
- **テストの期待値を緩めて通さない**

## 提出形式

結果を **`$RF_STEM-result.json`** に書いてください。**進行側は git だけを見ます。**

```json
{"items": [{"item_id": "I-002", "status": "fixed"}]}
```
