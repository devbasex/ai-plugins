[project]
name = "ndf-hook-trial"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
hook = ["tree-sitter==0.26.0", "tree-sitter-bash==0.25.1"]
sdk = ["claude-agent-sdk==0.2.160"]
bump = ["bump-my-version==1.5.1"]

[tool.uv]
package = false
