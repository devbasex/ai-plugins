{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'if [ -n \"$PLUGIN_ROOT\" ]; then python3 \"$PLUGIN_ROOT/scripts/serena-lsp.py\" hook session-start --client codex; fi; exit 0'",
            "timeout": 10,
            "statusMessage": "Checking Serena language servers"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash|shell|exec_command|local_shell|mcp__.*serena.*",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c '[ -x \"$0\" ] || exit 0; exec \"$0\" \"$@\"' \"$HOME/.cache/mcp-serena/roots$PLUGIN_ROOT/bin/python\" \"$PLUGIN_ROOT/scripts/serena-lsp.py\" hook pre-tool-use --client codex",
            "timeout": 5,
            "statusMessage": "Counting non-symbolic code reads"
          }
        ]
      }
    ]
  }
}
